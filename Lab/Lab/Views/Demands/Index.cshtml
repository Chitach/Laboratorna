@model IEnumerable<Lab.Models.Demand>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DemandName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LowerBorder)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UpperBorder)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DemandName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LowerBorder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpperBorder)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
